import re
import pytest
import socket
def test_readline():
    #launch me with pytest from root dir

    # server = MailServer()
    # TODO: make server.start()

    # socket = socket.socket()
    # with mock.patch('socket.socket') as mock_socket:
    #     mock_socet.return_value.recv.return_vslue =
    # address = 'localhost'
    # port = 2556
    # try:
    #     socket.connect((address, port))


    assert False